image:
  repository: rancher/local-path-provisioner
  tag: v0.0.28          # pinné — jamais latest

helperImage: busybox:1.36.1   # pinné aussi

storageClassName: local-path
setAsDefault: true
reclaimPolicy: Retain

nodePathMap:
  - /opt/local-path-provisioner