apiVersion: apps/v1
kind: Deployment
metadata:
{{ include "btop.header" . | indent 2}}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: btop-{{ .Release.Name }}
  template:
    metadata:
      labels:
        app: btop-{{ .Release.Name }}
    spec:
      hostPID: true
      containers:
        - name: btop-{{ .Release.Name }}
          image: {{ .Values.alpine_image }}
          command:
            - sh
            - -c
            - "apk add --no-cache btop ttyd && ttyd -p 7681 -i 0.0.0.0 btop --force-utf"
          env:
            - name: LANG
              value: "en_US.UTF-8"
            - name: LC_ALL
              value: "en_US.UTF-8"
          ports:
            - containerPort: 7681
          securityContext:
            privileged: true
          volumeMounts:
            - name: proc
              mountPath: /proc
            - name: sys
              mountPath: /sys
            - name: os-release
              mountPath: /etc/os-release
      volumes:
        - name: proc
          hostPath:
            path: /proc
        - name: sys
          hostPath:
            path: /sys
        - name: os-release
          hostPath:
            path: /etc/os-release