defaultNamespace: redis

helm:
  repo: https://lwtchami.github.io/helmcharts
  chart: xavki_redis
  releaseName: mytestredist
  valuesFiles:
    - values-common.yaml

targets:
  - name: redis-0-1-2
    clusterSelector:
      matchLabels:
        chart-redis: "0.1.2"
    helm:
      version: "0.1.2"
      valuesFiles:
        - values-0.1.2.yaml

  - name: redis-0-1-3
    clusterSelector:
      matchLabels:
        chart-redis: "0.1.3"
    helm:
      version: "0.1.3"
      valuesFiles:
        - values-0.1.3.yaml